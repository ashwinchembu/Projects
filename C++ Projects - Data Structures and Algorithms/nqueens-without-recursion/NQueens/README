# NQueens
## Resources Used:

 - Stack Implementation (codio resource provided by Prof. Demetrius)
## File Tree:
 - nqueens.cpp
	 - holds the necessary processes in order to parse the input files and give an appropriate result
 - setup.cpp
	 - isSafe - checks if square to place queen on is safe

## How to Run:

 1. ensure that the text file <input file> is in the proper folder and exists
 2. ensure that there is an input file that includes the proper formatting
 3. ensure that executables are made using "make" and "make clean"
 4. input ./nqueens < input > < output > to command line
 5. check output file for results

